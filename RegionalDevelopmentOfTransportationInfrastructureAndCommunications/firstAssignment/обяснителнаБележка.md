## Избрано летище:
	- Hartsfield–Jackson Atlanta International Airport (ATL)
		lat: 33,6367 (start_y)
		lon: -84,428101 (start_x)

---

## Стъпки на работа:
1. Извличане на всички дестинации (local_code) от избраното летище, с javaScript script
	- browserScript.js
2. Селектиране на всички записи по local_code, където е в списъка от дестинации (от стъпка 1) и създаване на нов слой airports
	- (Последният ред в browserScript.js се намира SQL куирито)
3. Създаване на нови колони start_x, start_y, както е показано във видеото.
4. Създаване на нов линеен слой (свързващ departure -> destination), както е показано във видеото.

---

### Проблеми, срещнати по време на работа:
1. Лейърът, изтеглен от World Airports ArcGisHub, не съдържа коректни данни: local_code is NOT UNIQUE; Също така 45 130 записа от 78 237 имат local_code = ' ' (space)
    - Заобиколил съм проблемът, като работя само със селектнатите данни. Ако е проблем ще потърся решение.

2. Тъй като сайта на летището не предоставя информация за frequency, съм използвал python скрипт за случайно определяне на честотата на полетите:
```python
import random

def assign_random_frequency():
	frequencies = ["high", "medium", "low"]
	return frequencies[random.randint(0, 2)]
```

---

**За да компенсирам двата малки недостатъка, съм изпълнил и additional suggestions.** 
Мисля че по този начин демонстрирам изцяло описания процес по задача 1.